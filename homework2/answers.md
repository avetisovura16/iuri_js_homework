1. its an input to collect information from user, it returns a string
2. input makes everything into a string so "42"
4. typof says you a type of an object. It helps to find bugs when you accidently use strings instead of numbers.
5. .trim() removes gaps, .toUpperCase() makes text big, and .toLowerCase() makes it small.
6. \n moves the text to the next line , \t makes a big gap between two parts of texts
7. sting texts does not makes sense for computer besides number is a thing pc can work with 
9. the only two possible values for boolean is true and false
10. == compares only values , and === compares both values and data types 
13. && means "and" , || means "or" , ! means "not" 
15. !true produces "false" 
16. && is usefull when you have two conditions , for example (value >= 18 && value <= 100 >) 
19. let is a changeable variable that you can change anytime, while const cannot be changed after you create it
